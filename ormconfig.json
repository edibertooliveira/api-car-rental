{
  "name": "default",
  "type": "postgres",
  "host": "localhost",
  "port": 5430,
  "username": "postgres",
  "password": "postgres",
  "database": "api-cars",
  "entities": [
    "./src/modules/**/typeorm/entities/*.ts"
  ],
  "migrations": [
    "./src/migrations/*.ts"
  ],
  "cli": {
    "migrationsDir": "./src/typeorm/migrations"
  }
}